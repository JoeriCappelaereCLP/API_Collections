info:
  name: MaterialGroups
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseUrl}}/sap/opu/odata/sap/API_PRODUCTGROUP_SRV/A_ProductGroup?$select=MaterialGroup"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: $select
      value: MaterialGroup
      type: query
  auth: inherit

runtime:
  variables:
    - name: BaseUrl
      value: https://s4-dev-api.clarebout.com

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
