info:
  name: KNA1
  type: http
  seq: 3

http:
  method: GET
  url: "{{BaseUrl}}/sap/opu/odata/sap/ZC_CA_MCT_CUSTMAST_GEN_CDS/zc_ca_mct_custmast_gen?$select=&$top=10"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: $select
      value: ""
      type: query
    - name: $top
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
